<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Application Status Update - {{companyName}}</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 700px;
      margin: 30px auto;
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }
    .header {
      text-align: center;
      padding: 25px;
      color: white;
      {{#if isApproved}}background: linear-gradient(135deg, #16a34a, #15803d);
      {{else if isRejected}}background: linear-gradient(135deg, #dc2626, #b91c1c);
      {{else}}background: linear-gradient(135deg, #2563eb, #1e40af);{{/if}}
    }
    .logo {
      width: 100px;
      margin-bottom: 10px;
    }
    .header h1 {
      margin: 10px 0 0;
      font-size: 24px;
    }
    .content {
      padding: 30px;
      color: #374151;
    }
    .status-box {
      background-color: #f9fafb;
      border-left: 5px solid
        {{#if isApproved}}#16a34a{{else if isRejected}}#dc2626{{else}}#2563eb{{/if}};
      padding: 15px;
      border-radius: 6px;
      margin: 20px 0;
    }
    .status-box strong {
      color: {{#if isApproved}}#16a34a{{else if isRejected}}#dc2626{{else}}#2563eb{{/if}};
      font-size: 18px;
    }
    .button {
      display: inline-block;
      padding: 12px 25px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      color: white;
      {{#if isApproved}}background-color: #16a34a;
      {{else if isRejected}}background-color: #dc2626;
      {{else}}background-color: #2563eb;{{/if}}
    }
    .footer {
      text-align: center;
      background-color: #f9fafb;
      padding: 20px;
      font-size: 13px;
      color: #6b7280;
    }
    @media (max-width: 768px) {
      .container {
        margin: 10px;
        width: auto;
      }
      .content {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="cid:company-logo" alt="{{companyName}} Logo" class="logo" />
      {{#if isApproved}}
        <h1>🎉 Congratulations!</h1>
        <p>Your dealership application has been approved</p>
      {{else if isRejected}}
        <h1>❌ Application Update</h1>
        <p>We appreciate your interest in {{companyName}}</p>
      {{else}}
        <h1>📋 Application Under Review</h1>
        <p>Thank you for your patience</p>
      {{/if}}
    </div>

    <div class="content">
      <p>Dear {{fullName}},</p>

      <div class="status-box">
        <p><strong>{{statusMessage}}</strong></p>
        <p>Application ID: <strong>{{applicationId}}</strong></p>
      </div>

      {{#if isApproved}}
        <p>
          Welcome to the {{companyName}} network! Our team will contact you shortly
          to complete your dealership onboarding process.
        </p>
        <div style="text-align: center; margin-top: 30px;">
          <a href="{{frontendUrl}}" class="button">View Next Steps</a>
        </div>
      {{else if isRejected}}
        <p>
          After careful review, we regret to inform you that your dealership application
          could not be approved at this time.
        </p>
        <p>
          You may reapply in the future when new opportunities become available.
        </p>
        <div style="text-align: center; margin-top: 30px;">
          <a href="{{frontendUrl}}" class="button">Explore Opportunities</a>
        </div>
      {{else}}
        <p>
          Your application is currently under review. You’ll receive another update soon
          once a final decision is made.
        </p>
      {{/if}}

      {{#if reviewNotes}}
        <div style="background-color:#fef3c7; border-left:4px solid #f59e0b; padding:15px; border-radius:6px; margin-top:20px;">
          <strong>Reviewer Notes:</strong>
          <p>{{reviewNotes}}</p>
        </div>
      {{/if}}

      <p style="margin-top: 25px;">
        For any queries, contact our support team:
      </p>
      <p>
        📧 <a href="mailto:{{contactEmail}}">{{contactEmail}}</a><br />
        ☎️ {{contactPhone}}
      </p>
    </div>

    <div class="footer">
      <p><strong>{{companyName}} - Dealership Program</strong></p>
      <p>© {{year}} {{companyName}}. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
